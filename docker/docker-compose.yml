services:
  database:
    image: 'postgres:latest'
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: diesel_demo
      POSTGRES_USER: app
      POSTGRES_PASSWORD: secret
  
